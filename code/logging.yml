formatters:
  simple:
    format: '[%(asctime)s][%(name)s][%(levelname)s] %(message)s'
handlers:
  console:
    class: logging.StreamHandler
    formatter: simple
    level: DEBUG
    stream: ext://sys.stdout
root:
  handlers:
  - console
  level: DEBUG
version: 1
